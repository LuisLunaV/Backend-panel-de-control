import dotenv from 'dotenv';
dotenv.config({path: '.env'});

const {
MYSQLPORT,
MYSQLHOST,
MYSQLDATABASE,
MYSQLPASSWORD,
MYSQLUSER,
CLIENT_BASE_URL,
JWT_SEED
} = process.env;
   
export{
MYSQLPORT,
MYSQLHOST,
MYSQLDATABASE,
MYSQLPASSWORD,
MYSQLUSER,
CLIENT_BASE_URL,
JWT_SEED
}